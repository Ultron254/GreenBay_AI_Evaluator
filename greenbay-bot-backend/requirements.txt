# Core Framework
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
pydantic>=2.10.0,<3.0
pydantic-settings>=2.7.0,<3.0

# LangChain & LangGraph
langchain>=0.3.0,<1.0
langchain-openai>=0.3.0,<1.0
langgraph>=0.2.0,<1.0
langchain-community>=0.3.0,<1.0

# LangSmith Monitoring
langsmith>=0.1.0,<1.0
sentence-transformers>=2.2.0,<3.0

# Tavily Search (for retail price lookup)
tavily-python

# Database
sqlalchemy>=2.0.0,<3.0
alembic>=1.14.0,<2.0
psycopg2-binary  # PostgreSQL adapter

# Vector Database
qdrant-client>=1.12.0,<2.0

# HTTP & API
httpx>=0.27.0,<1.0
requests>=2.32.0,<3.0
aiohttp>=3.11.0,<4.0

# WhatsApp Business API
python-multipart

# M-Pesa Daraja API
requests-oauthlib

# Image Processing
Pillow>=10.0.0,<11.0
python-magic

# System metrics (for the ops dashboard /dashboard/resources endpoint)
psutil>=5.9.0,<7.0

# PDF Generation (for receipts)
reportlab
weasyprint

# AWS S3 (for image storage)
boto3
botocore

# Utilities
python-dotenv
loguru>=0.7.0,<1.0
redis>=5.0.0,<6.0
celery

# Development & Testing
pytest
pytest-asyncio
black
isort
flake8

# Security
python-jose[cryptography]
passlib[bcrypt]
cryptography

# Date/Time
python-dateutil
pytz

# Environment
python-decouple

schedule

# Rate Limiting
slowapi

# Anthropic (Claude AI)
anthropic>=0.40.0,<1.0

# Image Quality Analysis (OpenCV)
opencv-python-headless

# Google Sheets Integration (CR-4)
gspread>=6.0.0
google-auth>=2.20.0

# Airtable Integration (backup data repository)
pyairtable>=2.0.0

# Google Vertex AI (Gemini) — we call the REST endpoint via httpx, so we only
# need OAuth from google-auth (already above). NOT pulling google-cloud-aiplatform
# here, because it transitively requires torch + full NVIDIA CUDA wheels (~1.5GB),
# which blows past the CI SSH deploy timeout and isn't needed for our usage.

# Video Processing (CR-1)
ffmpeg-python>=0.2.0

# Web scraping (for marketplace data)
beautifulsoup4>=4.12.0
